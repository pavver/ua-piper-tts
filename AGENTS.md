# AGENTS.md — UA-Piper-TTS

> Інструкції для ШІ-агентів по проекту UA-Piper-TTS.

---

## 📋 Про проект

- **Мета**: Генерація українського мовлення з тексту за допомогою Rust.
- **Платформа**: Linux aarch64 (Rockchip RK3588), також підтримується x86_64.
- **Технології**: Rust (основна логіка), Piper CLI (запуск підпроцесу).

---

## 🏗 Архітектура

```
┌─────────────┐     ┌────────────────┐     ┌────────────┐
│  Код Rust   │────▶│   normalize/   │────▶│ Piper CLI  │
│  main.rs    │     │ (пайплайн)     │────▶│ (підпроцес)│
└─────────────┘     └────────────────┘     └────────────┘
                            │                      │
                       Нормалізований         Файл WAV/MP3
                       текст (lowercase,      в output/
                       числа як слова,
                       наголоси)
```

### Пайплайн нормалізації (Tokenization Pipeline)

Нормалізатор використовує модульний трифазний пайплайн:
1. **Лексер** (`lexer.rs`): Розбиває текст на сирі фрагменти токенів (слова, числа, пунктуація, пробіли, час, дати).
2. **Парсер** (`parser.rs`): Аналізує сусідні токени для визначення смислових зв'язків (приєднання прийменників, одиниць виміру, масштабів числівників).
3. **Генератор** (`generator.rs`): Перетворює токени на стандартні українські фрази, застосовуючи правила граматики та наголоси.

### Основні компоненти

| Файл/Папка | Призначення |
|------------|-------------|
| `src/main.rs` | Точка входу, завантаження конфігурації. |
| `src/server.rs` | Веб-сервер (actix-web), генерація WAV/MP3. |
| `src/normalize/` | Пайплайн нормалізації: лексер, парсер, генератор, препроцесор. |
| `src/error_log.rs` | Запис помилок Piper TTS у файл `tts_errors.log`. |
| `download_models.sh` | Завантаження моделі Piper з HuggingFace. |
| `models/piper-uk_UK-dmytro-medium/` | ONNX модель та дані espeak-ng. |

### Залежності

- `hound` — робота з WAV.
- `num2words` (форк `num2words_v2`) — числівники з виправленою українською граматикою.
- `actix-web` — веб-фреймворк.
- `actix-files` — роздача статичних аудіофайлів.
- `serde` / `serde_json` — серіалізація та конфіги.
- `tokio` — асинхронний runtime.

### Зовнішні залежності

- **piper-tts** (`piper` CLI бінарник у системі).
- **ONNX Runtime** (встановлюється разом із piper-tts).
- **ffmpeg** (опціонально) — для кодування MP3 "на льоту" через stdout пайп.

---

## 🗣 Модель

### Piper `uk_UA-ukrainian_tts-medium`

- **Спікери**: 3 (0 — lada, 1 — mykyta, 2 — tetiana).
- **Типовий спікер**: `2` (tetiana, жіночий голос).
- **Частота**: 22050 Hz.

### Обмеження моделі

1. **Немає підтримки великих літер**: Текст обов'язково приводити до нижнього регістру перед синтезом.
2. **Помилка `Missing phoneme: П/С`**: Виникає, якщо текст містить великі літери.
3. **Наголоси**: Підтримуються через символ U+0301 (`це́льсія`).
4. **Дані espeak-ng**: Необхідні для фонемизації (мають лежати поруч із моделлю).

---

## 📝 Нормалізація тексту

### Правило для температури (°C)

- Якщо ціла частина **> 9**: використовується `"і [цифра]"` (наприклад, `36.6°C` ➔ `"тридцять шість і шість градусів це́льсія"`).
- Якщо ціла частина **≤ 9**: використовується `"цілих ... десятих/сотих"` (наприклад, `9.6°C` ➔ `"дев'ять цілих шість десятих градусів це́льсія"`).
- Для звичайних дробів (не температури): завжди `"цілих ... сотих"` (наприклад, `3.14` ➔ `"три цілих чотирнадцять сотих"`).

### Спеціальні символи

- `°C` / `°c` ➔ `градусів це́льсія`.
- `°F` / `°f` ➔ `градусів фаренгейта`.
- `°` (без літери) ➔ `градусів`.
- `%` ➔ `відсотків`.
- `+` ➔ `плюс`.
- `=` ➔ `дорівнює`.
- `&` ➔ `і`.

### Стани Home Assistant

- `on` ➔ `увімкнено`.
- `off` ➔ `вимкнено`.
- `open` ➔ `відчинено`.
- `closed` ➔ `зачинено`.
- `detected` ➔ `виявлено`.
- `clear` ➔ `чисто`.
- `true` ➔ `так`.
- `false` ➔ `ні`.

### Електричні одиниці

- `V` / `v` ➔ `во́льт`.
- `A` / `a` ➔ `ампе́р`.
- `W` / `w` ➔ `ва́т`.
- `Hz` / `hz` ➔ `ге́рц`.

### Апостроф

- `num2words` використовує U+02BC (`ʼ`), але Piper розуміє тільки U+0027 (`'`).
- Пайплайн автоматично замінює `ʼ` на `'`.

### Тисячні множники (к / k)

- Числа з суфіксами `к` або `k` множаться на `1000` (наприклад, `1.5к кг` ➔ `"тисяча п'ятсот кілограмів"`).

---

## 🐛 Історія виправлених помилок (Known Issues)

1. **Обрізання аудіо**: Перейшли з `sherpa-onnx` на `Piper` через погану обробку `add_blank` у VITS.
2. **Пропуск літер**: Текст переведено в нижній регістр (`.to_lowercase()`), оскільки модель ковтала перші великі літери.
3. **Неправильні наголоси**: Додано символ U+0301 у ключові слова (наприклад, `це́льсія`).
4. **Помилки невідомих символів**: Додано логування `error_log.rs` для запису помилок Piper CLI у `tts_errors.log`.

---

## 📐 Правила розробки (Development Rules)

1. **Rust + Piper CLI**: Жодних Python-скриптів у основному потоці.
2. **Нормалізація в Rust**: Використовувати тільки внутрішній пайплайн нормалізації.
3. **Обов'язковий нижній регістр**: Приводити весь текст до `.to_lowercase()` перед Piper.
4. **Наголоси**: Розставляти U+0301 для неоднозначних слів.
5. **Тести**: Будь-які зміни в логіці нормалізації мають супроводжуватися тестами в `src/normalize/mod.rs`.
6. **Чиста збірка**: Компіляція має проходити без warning-попереджень.
7. **Логування помилок**: Всі помилки Piper мають писатися в `tts_errors.log`.

---

## 🧪 Корисні команди

```bash
# Збірка
cargo build

# Запуск
cargo run

# Тести
cargo test

# Тести з виводом логів
cargo test -- --nocapture
```

---

## 🚀 Скрипти розгортання

- `scripts/install_deps.sh` — Встановлення системних залежностей.
- `scripts/build.sh` — Збірка (з підтримкою крос-компіляції).
- `scripts/deploy.sh` — Керування сервісом systemd.
- `scripts/update.sh` — Оновлення коду з GitHub та перезапуск.
- `scripts/quickstart.sh` — Швидкий старт однією командою.

---

*Останнє оновлення: 30.06.2026*
